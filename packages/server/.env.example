# JWT 密钥（必须修改为随机生成的 32+ 字符字符串）
# 生成方法：openssl rand -base64 32
JWT_SECRET=your-min-32-char-secret-key-here

# 数据库路径（SQLite）
DATABASE_URL=file:./dev.db

# 服务器端口
PORT=3000

# CORS 配置（生产环境建议指定具体域名）
CORS_ORIGIN=http://app.conan.js.cn
